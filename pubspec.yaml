name: flutter_realtime_chat
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  envied: 0.5.2
  models:
    path: packages/models
  http: 1.1.0
  web_socket_channel: 2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  envied_generator: 0.5.2
  build_runner: 2.4.6
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true